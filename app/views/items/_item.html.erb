
<%= item.photos.empty? ? link_to('Add photos',photos_path(id: item.id))  : image_tag(item.photos.avatar.url) %>
<div class="caption">
  <h3><%= item.name %></h3>
  <p>...</p>
  <p><a href="#" class="btn btn-primary" role="button">Button</a> <a href="#" class="btn btn-default" role="button">Button</a></p>
</div>



<!-- <li class="media">
	<a href="#" class='pull-left'>
		<%= image_tag '/assets/lovely_dress.jpg', class: 'media-objcet', size: '240x240' %>
	</a>
	<div class='media-body'>
		<h4 class='media-heading'><%= item.name %></h4>
		<%= item.description %><br>
		<%= number_to_currency(item.price, :unit => "€") %>
	</div>
</li> -->
		
